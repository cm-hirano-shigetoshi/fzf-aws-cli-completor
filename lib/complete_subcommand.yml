base_task:
  source: cat {{yml_dir}}/aws_subcommands/aws_*
  preview:
    command: 'bash {{yml_dir}}/../tools/show_help.sh aws $(echo {} | sed "s%/% %")'
    window: 'up:wrap'
  options:
    - reverse
    - query={{arg1}}
